# 后续 Todo

## P0：继续处理前必须完成

- 已完成：接入真实基金数据源，支持本地 `fundData` SQLite schema。
- 已完成：增加 `fundData` schema 适配测试和真实库副本 smoke。
- 已完成：补基础收益风险特征：区间收益、年化收益、波动、最大回撤、夏普，并落 `feature_values`。
- 已完成：补行业集中度和权益仓位标签，先用明确阈值，不做复杂风格推断。
- 已完成：新增 `label_calculation_states`，记录 `triggered` / `not_triggered` / `not_computed`，能解释标签为什么没出来。
- 已完成：批次摘要增加计算状态分布和未计算原因分布，导出增加 calculations 表。
- 已完成：按 1M / 3M / 1Y / 3Y 窗口计算收益风险特征，正式收益风险标签优先使用 3Y，其次使用 1Y。
- 已完成：新增基金分类和业务分组层，输出权益相关、主动/被动、标签可计算、风格清晰度，以及主动权益候选池、被动指数工具池、数据缺口池等分组。
- 已完成：分类和分组已落库、可在单基金报告和批次导出中查看。
- 继续补：真实跑批时补更长 NAV 历史，让更多老基金可以产出 3Y 标签。
- 继续补：增加真实数据适配测试的数据缺失样本，覆盖缺净值、缺持仓、缺费率、缺经理。
- 继续补：固定一批真实基金样本，输出“标签、证据、计算状态、分类、分组、未计算原因”的验收报告。
- 增加真实跑批验收样例：固定 5 只基金，导出“标签、证据、计算状态、未计算原因”的样例报告。

## P1：做成可用工作台

- 建一个最小前端页面：批次列表、基金标签明细、证据、覆盖率、计算状态。
- 前端增加分类/分组筛选：按主动权益候选池、被动指数工具池、数据缺口池、风格因子缺失池查看基金。
- 增加导出功能：导出单次 run 的标签 CSV 或 Excel。
- 增加规则配置文件：把阈值从代码默认值迁移到 YAML/JSON，再注入 `RuleConfig`。
- 增加标签启停：禁用的标签不参与计算，但保留历史结果可查。

## P2：股票因子和高级风格

- 建设真实 `stock_factors` 生成流程：PB、ROE、股息率、成长性、市值、估值分位、行业、主题。
- 做持仓穿透聚合：按持仓权重聚合股票因子到基金层。
- 先输出观察型风格暴露，不直接输出正式风格标签。
- 有足够样本和复核证据后，再启用 `deep_value`、`quality_growth`、`dividend_stable` 等正式标签。
- 增加风格稳定性：多期持仓和因子暴露变化。

## P3：工程化和审计

- 增加 Alembic 或轻量迁移机制，管理 SQLite schema 演进。
- 增加数据质量报告：缺失字段、异常值、过期数据、报告期错配。
- 增加运行日志和错误表：保留单只基金失败原因。
- 增加规则回放：指定历史 run、规则版本和数据日期重算。
- 增加 CI：pytest、类型检查、最小 API smoke test。

## 暂缓事项

- 暂缓 LLM 自动分析结论。
- 暂缓复核队列、人工确认、权限和复核工作台。
- 暂缓完整组合优化、FOF 配置和交易回测。
- 暂缓接入复杂前端权限系统。
- 暂缓直接复用外部仓库业务代码。
